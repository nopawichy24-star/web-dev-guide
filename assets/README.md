# Assets

วางไฟล์เพิ่มเติมในโฟลเดอร์เหล่านี้:

- `images/` — รูปภาพ เช่น PNG, JPG, WebP, SVG
- `icons/` — ไอคอนและ favicon
- `fonts/` — ฟอนต์ที่โปรเจกต์เป็นเจ้าของหรือมีสิทธิ์ใช้งาน
- `files/` — PDF หรือไฟล์ดาวน์โหลดอื่น ๆ

ไฟล์ `.gitkeep` มีไว้เพื่อให้ GitHub เก็บโฟลเดอร์ที่ยังว่างอยู่
